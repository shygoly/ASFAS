# DECISIONS.md — 决策登记册（ID 命名空间唯一权威）

> **本文件是登记册，不是论证书，也不是日志。** 每条只占一行：ID + 一句话 + 结论 + 出处。
> **论证与推导留在出处文档**，不在此复制——复制即制造第二个事实来源。
>
> **机器校验**：`{{校验命令}}`（已挂进 `{{默认测试命令}}`）。
> 代码或元语文档里出现的 ID **必须在本册**，否则测试红。
>
> **新增决策**：按对应命名空间递增，写进本册 + 在出处文档补论证，**同一提交**。
> **待定项唯一位置 = 本册的"待定"节**。决出后入册 + 销项，**同一提交**。

---

## 0. 命名空间（**先看这张表，避免找错地方**）

> 命名空间的区分性必须在**视觉层面**成立——查册靠模式匹配完成，
> 前缀视觉相似（如 `D-n` 与 `D-xxA`）会导致查错地方。

| 前缀 | 含义 | 出处（论证在此） | 数量 |
|---|---|---|---|
| `INV-n` | **不变量**：任何实现都不得违反的硬约束 | {{出处文档}} | {{n}} |
| `ADR-n` | **架构决策**：工程层不可逆选择 | [`ARCHITECTURE.md`](ARCHITECTURE.md) | {{n}} |
| `B-n` | **边界规矩**：模块/包之间的硬边界 | [`ARCHITECTURE.md`](ARCHITECTURE.md) | {{n}} |
| `DS-n` | **UI 反模式**：呈现层禁止事项 | [`DESIGN.md`](DESIGN.md) | {{n}} |
| `{{前缀}}` | {{含义}} | {{出处}} | {{n}} |

---

## 1. INV — 不变量

> 子编号 `INV-n.m` 是测试用例粒度，**校验器按父编号查册**。

| ID | 内容 | 守护层 |
|---|---|---|
| `INV-{{n}}` | {{一句话内容}} | {{service ｜ DB 约束 ｜ 触发器 ｜ 状态机}} |

---

## 2. ADR — 架构决策

| ID | 内容 | 状态 |
|---|---|---|
| `ADR-{{n}}` | {{一句话结论}} | {{已守护 ｜ 未配 ｜ 待决策}} |

---

## 3. B — 边界规矩

| ID | 内容 | 状态 |
|---|---|---|
| `B-{{n}}` | {{一句话边界}} | {{守护方式或"评审把关"}} |

---

## 4. DS — UI 反模式

| ID | 内容 | 状态 |
|---|---|---|
| `DS-{{n}}` | {{一句话反模式}} | {{守护方式}} |

---

## 5. {{业务/技术决策点命名空间}}

| ID | ★ | 议题 | 结论 |
|---|---|---|---|
| `{{ID}}` | {{★ 表示影响数据模型/架构}} | {{一句话议题}} | {{结论}} |

---

## 6. 决策 ↔ 实现的已知偏离（**只写事实**）

> 在册结论与代码现状不一致的地方。**不修改决策结论**（那要走重新拍板），只登记偏离。
>
> **这张表的价值：智能体看到登记册与代码不一致时，不会自作主张去"修正"代码。**
>
> `性质` 列不可省略——*已改主意* 意味着代码是对的、登记册待更新；
> *未实现* 意味着登记册是对的、代码待补。**两者的修复方向完全相反。**

| ID | 在册结论 | 代码现状 | 性质 |
|---|---|---|---|
| `{{ID}}` | {{登记的结论}} | {{代码实际做法}} | {{已改主意 ｜ 未实现 ｜ 部分实现}} |

---

## 7. 待定（**唯一位置**）

> 其他文档只可指向本节，不得各自维护未决清单。
> 每条必须写明"决出前禁止做什么"——**倾向本身会诱导预埋**。

| # | 事项 | 倾向 | 决出前禁止 |
|---|---|---|---|
| `{{ID}}` | {{一句话事项}} | {{倾向或 —}} | {{禁止做什么}} |

---

## 附录：格式契约（校验器解析规则）

1. §1–§5 的表格首列是 ID，须用**反引号**包裹（使 grep 可区分 ID 与普通文本）。
2. 代码与元语文档中出现的 ID（**词边界**匹配）必须在册 —— 硬失败。
3. `INV-n.m` 按父编号 `INV-n` 查册。
4. 在册但全仓零引用 → **WARN，不红**（纯业务决策可以不落代码）。
5. 本册与各元语中的相对路径链接目标必须存在。
6. **任一登记表解析为 0 行 = 格式契约破坏 = 红。**
