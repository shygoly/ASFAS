# DESIGN.md — UI 规范与术语（Source of Truth）

> **本文件的表格是解析目标**（术语注册表、反模式表、token 源）。
> 破坏格式 / 违反规矩 = **测试红**（`{{校验命令}}`，已挂进 `{{默认测试命令}}`）。
>
> 关联：[`ARCHITECTURE.md`](ARCHITECTURE.md)（分层）· [`DECISIONS.md`](DECISIONS.md)（`DS-n` 在册）。
>
> **只写事实**：写 UI **现在**的规范。未消费的 token 与组件不得预填。
>
> **边界**：架构问题（字段契约、分层职责、数据形状）**不写进本文件**——
> 这类越界登记为反模式并接受评审把关。

---

## 1. 设计 token 唯一源

| 类别 | 唯一源位置 | 说明 |
|---|---|---|
| {{颜色 ｜ 间距 ｜ 字号 …}} | `{{文件路径}}` | {{说明}} |

**硬规矩**：UI 源码中禁止 {{色值/间距}} 字面量散点，一律走 token。

---

## 2. 术语注册表

> 在专业领域，同义词不是风格问题而是**语义错误**。
> 智能体的语言模型倾向用同义词增加表达多样性，这在此处恰好是有害的。

| 概念 | 唯一用词 | **禁用的同义词** | 禁用理由 |
|---|---|---|---|
| {{概念}} | {{用这个词}} | {{不用这些词}} | {{为什么——通常是它们各自另有精确含义}} |

---

## 3. 页面 / 布局模板

| 模板 | 用途 | 实现位置 |
|---|---|---|
| {{模板名}} | {{用途}} | `{{路径}}` 或 `未实现` |

> 声明的模板须有对应实现，或标注"未实现"并在 [`DECISIONS.md`](DECISIONS.md) 待定节登记。

---

## 4. 反模式

| ID | 内容 | 守护方式 |
|---|---|---|
| `DS-{{n}}` | {{一句话反模式}} | {{grep ｜ 组件测试 ｜ 评审把关}} |

---

## 5. 可访问性底线

| 要求 | 判据 | 守护 |
|---|---|---|
| {{要求}} | {{可测的判据}} | {{守护方式}} |

---

## 附录：格式契约（校验器解析规则）

1. §1 token 唯一源声明的文件须存在；UI 源码中的 {{字面量}} grep 命中须为 0。
2. §2 术语注册表的**禁用词**在 UI 源码中出现 = 红。
3. §3 声明的模板实现路径须存在，或显式标注"未实现"。
4. §4 反模式 ID 须在 [`DECISIONS.md`](DECISIONS.md) 在册；每条须有守护方式列。
5. **任一契约表解析为 0 行 = 格式契约破坏 = 红。**
