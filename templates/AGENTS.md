# AGENTS.md — {{项目名}} 工作入口

> **入口指针，不是元语。** 本文件**不含任何一手事实**，只含指针、阅读规则、硬规矩与命令。
> 任何在本文件中出现的一手事实即为违规——它属于某本元语。
>
> **Source of Truth 六元语**（根目录，本文件是入口指针，**不复制**其内容）：
> [`ARCHITECTURE.md`](ARCHITECTURE.md)（分层与边界）· [`DESIGN.md`](DESIGN.md)（UI 规范与术语）·
> [`DOMAIN.md`](DOMAIN.md)（数据契约）· [`FLOWS.md`](FLOWS.md)（流程契约）·
> [`DECISIONS.md`](DECISIONS.md)（ID 登记册，唯一权威）· [`DEPLOY.md`](DEPLOY.md)（端口与 env）。
>
> {{条件元语存在时补一行：另有 `AGENT-OPS.md` 治理"写这个仓库的你" / `AI.md` 治理产品里的 AI}}
>
> **与旧文档冲突以元语为准，冲突时停下说明，不要悄悄改。**
> {{有取代关系时：**取代声明**：本文件取代 {{日期}} 的 {{版本描述}}。}}

## 元语契约必读规则（机器校验，违反 = 测试红）

**元语里的表格与代码块是解析目标，破坏格式 = 测试红。** 汇总命令 `{{校验命令}}`（已挂进 `{{默认测试命令}}`）。

<!-- 每条三部分：① 触发条件（什么时候要读）② 切片指引（读哪节，勿全文加载）③ 陷阱提示（该元语特有的、容易误判的地方）。
     第三部分最容易被省略，也最有价值 —— 它是让智能体避免"看起来正确的错误改动"的关键。 -->

- **`DOMAIN.md`** — 改 {{schema / 数据层}} 前，**按聚合切片读对应 `##` 小节**（勿全文加载）。
  加对象/加枚举/改枚举取值须同步该小节；枚举取值**顺序敏感**。
  {{松弛点节}}记录了故意的不一致——**那些不是 bug，别顺手"修"**。
- **`FLOWS.md`** — 改任何状态写入前先读对应 `flow` 块。新增/修改转移须同步五段；
  **`impl` 是状态级语义，不是转移级**。`impl: no` 的状态必须在处置节有条目。
- **`DECISIONS.md`** — 代码里写任何 ID（词边界）**必须先在册**。
  {{已知偏离节}}记录**决策与实现的有意偏离**——读到不一致先查这张表，**别自作主张"修正"代码**。
  **待定项唯一位置 = {{待定节}}**，决出后入册 + 销项同一提交。
- **`DEPLOY.md`** — 改端口或新增读取的环境变量前**先改本文件**，再改样例与代码。
- **`ARCHITECTURE.md` / `DESIGN.md`** — 边界与 UI 规范的**论证**在此；结论已收进 `DECISIONS.md`。

## 项目一句话

{{是什么系统 + 当前进展阶段。**不写数字**——写数字的文档注定漂移。}}

## 技术栈锁定（禁止引入未列出的同类替代品）

- {{类别}}：{{选定的栈}}
- 已装未启用：{{无（不预埋）}}

## 仓库地图与边界（每行一句）

- `{{目录}}/` — {{一句话职责}}
- **硬边界**：{{摘要}}。详见 [`ARCHITECTURE.md`](ARCHITECTURE.md) §分层与边界。

<!-- 不写模块数量、文件行数一类数字；以 `ls` 为准或写结构描述。 -->

## 标准工作流（先看邻近文件仿照，再动手）

- **{{改数据形状}}** = {{动作序列}}
- **{{新增业务模块}}** = **黄金纵切：照抄 `{{参照目录}}`**（{{构成文件清单}}）
- **{{合规敏感路径}}** = 必须显式声明触及哪条 INV

## 硬规矩（违反 = bug）

- {{规矩}}
- 大改先给 3–5 行方案；**先读邻近文件仿照，不凭记忆写**。
- 不新增依赖（先确认现有栈无法满足）；不预埋"以后可能用到"的代码/配置。
- 元语说的和代码做的不一致时，**停下说明冲突**，不要单方面改任一侧。
- 看到测试红，**先在干净基线复跑做归属判断**，再动手。

## 禁止行为

- {{本项目的禁止清单}}
- 不手改生成产物；不改已应用的迁移。

## 命令

```bash
{{命令}}          # {{说明}}
```
