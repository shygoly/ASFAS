# {{项目名}} — 架构设计（Source of Truth）

> **依据/取代关系声明**：本文件是架构层的唯一事实源。与 `{{旧设计文档路径}}` 冲突时，
> **以本文件为准**；旧文档降为历史参考（立项/评审输入），不再代表当前代码现实。
>
> **机器校验**：`{{校验命令}}`（已挂进 `{{默认测试命令}}`）。
>
> 关联：[`DESIGN.md`](DESIGN.md)（UI 规范）· [`DOMAIN.md`](DOMAIN.md)（数据契约）·
> [`FLOWS.md`](FLOWS.md)（流程契约）· [`DECISIONS.md`](DECISIONS.md)（ID 登记册）·
> [`DEPLOY.md`](DEPLOY.md)（端口与 env）。
>
> **读者判别标准**：读完任一节，你能回答"**我违反了会怎么被抓住**"，
> 而不是"作者建议我怎么做"。每条硬规矩必须能落到 grep / 约定测试 / CI 门禁三选一——
> **写不出守护方式的规矩会被删**。
>
> **只写事实**：写代码**现在**的样子。应该的样子进 [`DECISIONS.md`](DECISIONS.md) 待定节。

---

## §0 决策摘要（已锁定）

> 每行必须带**锁定依据**，指向具体配置文件——否则无法判断这条是否还成立。

| 议题 | 结论 | 锁定依据 |
|---|---|---|
| {{议题}} | {{结论}} | `{{配置文件路径}}` |

---

## §1 目标与非目标

**目标**
- {{一句话目标}}

**非目标（防镀金，违反这些会被拒绝合并）**
1. **{{非目标}}**：{{为什么不做，以及不做到什么程度}}

> 非目标必须与目标对称陈述，且写明拒绝后果。

---

## §2 总体架构

{{组件与数据流图。属叙述段，不被解析。}}

**限界上下文**

| 模块 | 职责（一句话） | 关键依赖 |
|---|---|---|
| `{{模块}}` | {{职责}} | {{依赖}} |

---

## §3 分层与边界

| 层 | 目录 | 职责（一句话） |
|---|---|---|
| {{层名}} | `{{目录}}` | {{职责}} |

**硬边界（每条带守护方式；违反 = CI 红）**

| # | 边界 | 守护方式 |
|---|---|---|
| `B-{{n}}` | {{边界内容}} | {{grep 表达式 ｜ 约定测试 ｜ CI 门禁}} |

> 无守护方式的边界不予合并。

---

## §4 配置 / 环境

**加载机制**：{{机制描述}}

> **键位清单归 [`DEPLOY.md`](DEPLOY.md)**，此处只写机制。spec/文档记约定，不记值。

---

## §5 不变量与守护点

> 以下 INV 是**合规/业务底线，不是建议**。违反任一条 = 数据完整性 bug。

| 不变量 | 含义 | 守护点 |
|---|---|---|
| `INV-{{n}}` | {{含义}} | `{{路径#符号}}` 或 `{{DB 对象}}` |

**硬规矩**：改动{{敏感路径}}时，必须显式声明触及哪条 INV；无 INV 声明的改动不予合并。

---

## §6 硬约定 ADR（每条"违反 = bug"+ 守护方式）

| ADR | 内容 | 守护 |
|---|---|---|
| `ADR-{{n}}` | {{内容}} | `{{守护脚本}}`；{{已落地 ｜ 未配}} |

---

## §7 待决策项（**禁止预埋偏向**）

> 本节只做指针，未决项唯一位置 = [`DECISIONS.md`](DECISIONS.md) 待定节。

见 [`DECISIONS.md`](DECISIONS.md) §待定。

---

## §8 分阶段落地

| Phase | 主叙事（一句话） | 交付 | 状态 | 执行计划 |
|---|---|---|---|---|
| {{Pn}} | {{一句话}} | {{交付物}} | {{✅ ｜ 🚧}} | [{{计划名}}]({{路径}}) |

> **阶段性执行计划不是元语。** 完成后显式降为历史快照——
> 当前事实以元语 + 代码为准。

---

## 附录：格式契约（校验器解析规则）

1. §0 决策摘要每行的锁定依据文件须存在。
2. §3 分层表声明的目录须真实存在。
3. §3 硬边界表每条声明的守护方式须可定位（脚本存在 / grep 可执行 / CI job 存在）。
4. §5 不变量的守护落点（`路径#符号` 或 `路径:行`）须可解析。
5. 本文件中出现的 `ADR-n` / `B-n` / `INV-n` 须在 [`DECISIONS.md`](DECISIONS.md) 在册。
6. 相对路径链接目标须存在。
7. **任一契约表解析为 0 行 = 格式契约破坏 = 红。**
