# DEPLOY.md — 端口与环境变量（Source of Truth）

> **本文件的表格是解析目标**，与 `{{环境样例文件}}`、`{{编排文件}}`、代码读取点做**三方对拍**。
> 不一致 = **测试红**（`{{校验命令}}`，已挂进 `{{默认测试命令}}`）。
>
> **改端口 / 新增读取的环境变量时，先改本文件**，再改样例文件与代码。
> 元语先行把对拍从事后检查变成事前设计。
>
> 关联：[`ARCHITECTURE.md`](ARCHITECTURE.md)（配置加载机制）· [`DECISIONS.md`](DECISIONS.md)。
>
> **真实值只进 `{{本地覆盖文件}}`（已忽略提交）。样例文件只放键位与占位，不放任何真值。**

---

## 1. 端口

| 端口 | 服务 | 来源 | 说明 |
|---|---|---|---|
| `{{端口}}` | `{{服务名}}` | {{env 键 ｜ 编排文件 ｜ 框架默认}} | {{说明}} |

### 1.1 本地依赖事实

> 记录**实际发生了什么**，不是应该怎样。这类事实只能写在元语里——
> 代码看不出来、编排文件也看不出来，只有记下来才能阻止下一个人重新踩一遍。

| 服务 | 获取方式 | 启停 | 事实 |
|---|---|---|---|
| `{{服务}}` | {{如何装/起}} | {{命令}} | {{监听地址、是否共享实例、已知坑}} |

---

## 2. 环境变量（部署面）

| 键 | 面 | 必需 | secret | 缺省 | 说明 |
|---|---|---|---|---|---|
| `{{KEY}}` | {{api ｜ web ｜ worker}} | {{是/否}} | {{是/否}} | {{缺省值或 —}} | {{用途}} |

> `secret` 列不可省略——使"哪些键是密钥"成为可查事实而非默认知识。

---

## 3. 环境变量（测试面）

| 键 | 必需 | 缺省 | 说明 |
|---|---|---|---|
| `{{KEY}}` | {{是/否}} | {{缺省}} | {{用途}} |

---

## 4. 生产环境未落地要求

> 已知但尚未实施的生产要求。登记在此，避免上线时才发现。

| # | 要求 | 现状 |
|---|---|---|
| {{n}} | {{要求}} | {{未实施的说明}} |

---

## 5. 忽略名单

> 不参与三方对拍的工具类变量。名单本身须非空可解析。

| 键 | 为什么忽略 |
|---|---|
| `{{KEY}}` | {{如：运行时/CI 注入的通用变量，不属本项目配置面}} |

---

## 附录：格式契约（校验器解析规则）

1. §2 ∪ §3 的键位集合 **==** 环境样例文件的键位集合 **==** 代码 `{{读取方式}}` 的键位集合（**三方相等**）。
2. §5 忽略名单不参与对拍。
3. §1 中标注来源为编排文件的端口，须在该编排文件的端口声明中出现。
4. **扫描面**必须覆盖全部会读取环境变量的位置，包括前端构建配置、测试辅助代码与脚本。
   扫描面在校验器中显式声明，并登记于此：`{{扫描面清单}}`。
5. **任一契约表解析为 0 行 = 格式契约破坏 = 红。**
