# AI.md — 产品侧模型能力契约（Source of Truth）

> **条件元语。触发条件：产品交付面包含模型推理或生成能力。** 不满足则**不得创建**。
>
> **治理对象 = 产品里的 AI。** 不是写这个仓库的 coding agent——
> 后者归 [`AGENT-OPS.md`](AGENT-OPS.md)，**两者不可合并**：
> 前者约束**产品向用户承诺什么**，后者约束**开发过程如何被信任**。
>
> **机器校验**：`{{校验命令}}`（已挂进 `{{默认测试命令}}`）。
>
> 关联：[`ARCHITECTURE.md`](ARCHITECTURE.md)（分层）· [`DECISIONS.md`](DECISIONS.md)（决策在册）·
> [`DESIGN.md`](DESIGN.md)（用户可见性的呈现规范）。
>
> **只写事实**：写当前**真的在用**的模型与 prompt。计划中的进 `DECISIONS.md` 待定节。

---

## 1. 模型清单

| 模型标识 | 用途 | 调用点 | 降级目标 |
|---|---|---|---|
| `{{模型标识}}` | {{用途}} | `{{路径#符号}}` | {{降级到哪个模型/规则/人工}} |

---

## 2. Prompt 资产

| Prompt | 位置 | 消费者 | 输入契约 | 输出契约 |
|---|---|---|---|---|
| `{{名称}}` | `{{路径}}` | `{{调用点}}` | {{schema 或描述}} | {{schema}}；parse 点 `{{路径#符号}}` |

> 输出契约必须有实际的 parse 点——**没有 parse 点的"输出契约"是愿望不是契约**。

---

## 3. 不确定性边界

> 明确列出模型输出**不得单独决定**的事项。
> 这是 `AO-1`（确定性的禁止交给模型）在产品侧的镜像。

| # | 模型输出**不得单独决定** | 中间必须有什么 | 守护 |
|---|---|---|---|
| {{n}} | {{如：权限、金额、医疗结论、合规判定}} | {{确定性校验 ｜ 人工确认}} | {{grep 禁止面 ｜ 测试}} |

---

## 4. 降级策略

| 触发 | 行为 | 用户可见性 | 测试 |
|---|---|---|---|
| 模型不可用 | {{行为}} | {{是否告知用户}} | `{{测试标识}}` |
| 超时 | {{行为}} | {{}} | `{{测试标识}}` |
| 输出不合契约 | {{行为——不得容错解析}} | {{}} | `{{测试标识}}` |

> 每条降级分支须有对应测试——未被测试的降级分支在真正需要时通常不工作。

---

## 5. 用户可见性

| 场景 | 是否须标示"由 AI 生成" | 呈现方式 |
|---|---|---|
| {{场景}} | {{是/否}} | {{参照 `DESIGN.md`}} |

---

## 6. 评测集

| 评测 | 数据集位置 | 阈值 | 跑在哪 |
|---|---|---|---|
| {{评测名}} | `{{路径}}` | {{阈值}} | {{CI ｜ 手动}} |

---

## 7. 已知松弛点

| # | 内容 | 为什么保留 | 收敛条件 |
|---|---|---|---|
| `{{id}}` | {{}} | {{}} | {{}} |

---

## 附录：格式契约（校验器解析规则）

1. §1 模型标识须能在代码中定位；调用点 `路径#符号` 须可解析。
2. §2 prompt 位置须存在；输出契约的 parse 点须可解析。
3. §3 的禁止面 grep 命中须为 0。
4. §4 每条降级分支的测试标识须存在。
5. §6 评测数据集路径须存在。
6. 文档中的决策 ID 须在 [`DECISIONS.md`](DECISIONS.md) 在册。
7. **任一契约表解析为 0 行 = 格式契约破坏 = 红。**
