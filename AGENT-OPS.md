# AGENT-OPS.md — coding agent 治理契约（Source of Truth）

> **条件元语。触发条件：仓库代码由 coding agent 参与编写。** 本仓满足——全部提交由 Claude Code 会话产出。
>
> **治理对象 = 写这个仓库的 coding agent。** 不是编排面上那些尚未落地的角色（`supervisor`/`impl`/`test`/
> `reviewer`/`guard`）——它们不在 §1 在册，因为 §1 只登记**已存在**的角色形态。
>
> **机器校验**：`npm run verify:agent-ops`（已挂进 `npm test`）。
> 本文件的表格是解析目标，**破坏格式 = 测试红**。
>
> 关联：[`spec/ASFAS.html`](spec/ASFAS.html) §27（本文件的内容模型）· [`README.md`](README.md)。

---

## 0. 为什么本仓需要这本

本仓是**治理工厂**：它产出的东西是别的项目的闸门。一个自己没有动作分级的工厂去要求别人做动作分级，
是 `FP-1` 意义上的两个事实来源——文档说的和自己做的不一致。

更具体的驱动力是 `AG-10`：**护栏阈值 MUST 在编排面上线之前确定并在册**。
编排面（§28–§33、§41）正在建，阈值现在不定，上线时就会临场拍数字，而临场拍的数字永远偏松。

不做的后果不是抽象的。本仓已经踩到过两次：一次是供应商路由被宿主凭证静默劫持
（活动会跑在错账户上而血缘记的是路由表里那个），一次是计费错误被误归 `transient` 导致无限重试。
两次都不是"写错代码"，是**没有把判据写下来**。

---

## 1. 能力注册表（**闭集**）

> 只登记**已存在**的角色形态。尚未落地的角色**不得预登记**。
> 新增角色 = 改本表 + 落代码，**同一提交**。

| 角色 id | 形态 | 允许的工具面 | 最高动作等级 | 血缘 |
|---|---|---|---|---|
| `claude-code-session` | 单 agent 串行 | 读写工作区文件 · 跑 shell 命令 · 网络检索 · MCP（serena / playwright） | `L2` | 可选 |
| `research` | 单 agent 串行 | **空集**（`tool_grants: []`，只做模型推理，不授予任何工具） | `L0` | 必须 |
| `supervisor` | 单 agent 串行 | **空集**（`tool_grants: []`，L0 只读；分析产出任务清单，不写文件） | `L0` | 必须 |
| `test` | 单 agent 串行 | 读写工作区文件 · 跑 shell 命令（TDD Test 阶段，在项目 L1 面写测试） | `L1` | 必须 |
| `impl` | 单 agent 串行 | 读写工作区文件 · 跑 shell 命令（TDD Impl 阶段，改动集经路径级放行合并，D-RELEASE-1） | `L2` | 必须 |

> 最高动作等级**不得为 `L3`**——`L3` 不设放行路径。
>
> `research` 由 `factory/orchestration/workflows/research.mjs` 派发，产出是决策候选而非代码（`WF-24`）。
> 它进本表是因为**它已经跑过活动并产生了血缘**——`LN-4` 会拿血缘里的 `agent.role` 反查本表，
> 落地了不登记即红。
>
> `supervisor` 由 `factory/orchestration/workflows/orchestrate.mjs` 的 `dispatch` 阶段派发——
> 该阶段**不标 `deterministic`**（开放式缺口分析，不是查表执行），产出是 typed 任务清单。
> 在 `research` 工作流里 supervisor 仍是 `deterministic`（查表执行，不派活动，`AG-17`）。
> typed 输入输出 schema 见 `orchestrate.mjs` 的 `ORCHESTRATE_OUTPUT_SCHEMA`；工具面为空集
> （`AG-16`：Supervisor MUST 是 L0，无文件写入能力）。

---

## 2. 动作分级

> 按**改动的可逆性与影响面**分级，**不按改动规模**（`AG-4` / `AG-5`）。
> 一个字符的改动可把 fail-closed 翻成 fail-open；重构一千行测试可能零风险。

| 等级 | 范围（路径面） | 策略 |
|---|---|---|
| `L0-readonly` | 读文件 · 跑 `npm test` 与各 `verify:*` · 查 git 历史 · 只读检索 | agent 自主 |
| `L1-low-write` | `README.md` · `.serena/memories/**` · `runtime/*/smoke.mjs` · 元语的**非契约段落** | agent 自主 + 事后审计 |
| `L2-high-write` | `spec/**` · `factory/**` · `templates/**` · `projects/registry.json` · `runtime/*/adapter.mjs` · `runtime/*/providers.mjs` · `package.json` · `.gitignore` · 元语的**契约表格** | **强制人工放行** |
| `L3-forbidden` | 见 §3 | **不设放行路径** |

**归级规则**（`§27.6`）：未匹配任何路径面的路径归 `L2`（fail-closed，`AG-14`）；
路径面冲突取**最具体**的匹配，同具体度取**更高**等级（`AG-15`）；`L3` 命中**短路**。

**为什么 `runtime/*/providers.mjs` 是 `L2`**：它决定哪个活动用哪个账户的凭证。
改错不会报错，会**静默把活动路由到错误的供应商**，而血缘记录的是路由表里写的那个——
这是证据失真，不是功能 bug。

**`L2` 的人工放行当前形态**：交互式会话中由人逐条确认（Claude Code 的权限提示）。
无平台时间戳、无独立放行记录——这是缺口，见 §7 `X-1`。

---

## 3. L3 禁令（**不提供人工放行档**）

> 判据（`AG-6`）：**即使有人批准，这个动作本身仍然违规，或造成不可逆的证据损坏。**
> 不满足此判据的**不得**放进 `L3`（应归 `L2`）——把流程偏好写成禁令会产生无人遵守的摆设规则。

| # | 禁令 | 为什么放行也不行 | 机器守护 |
|---|---|---|---|
| `F-1` | 把真实密钥写进**入库文件** | 不可撤回——git 历史永在，轮换密钥也删不掉已推送的记录 | 仅文档约束 + `.gitignore`；见 §7 `X-2` |
| `F-2` | 删改负向验证用例、或削弱已有校验维度的断言，使闸门仍显示绿 | **假绿比没有闸门更危险**：它让人以为该风险已被覆盖。与 `§27.3` 的"给不变量测试加跳过标记"同构 | 负向验证自身（注入 → 须红）；覆盖率守护见 §7 `X-4` |
| `F-3` | 修改或删除已写入的血缘记录 | 血缘是受监管记录，事后可改就等于没有（`FINV-7`） | `LN-2`：`git diff --diff-filter=MD` 对 `.agent-runs/` 零命中；进程内 `writeRecord()` 拒绝覆盖。仅对**已提交**记录生效，见 §7 `X-6` |

> **不在 L3 的三条**，按 `AG-6` 重判后归 `L2`：给校验器加 `--fix`（违反 `VC-2`，但可经修宪改变，是流程偏好）·
> 直接改 `spec/ASFAS.html`（宪法可修，须放行）· 绕过闸门提交（流程偏好，参照规范把"直推主分支"重判为 `L2`）。

---

## 4. 决策血缘

每次 agent 会话落**一份**记录：`.agent-runs/<UTC 日期>/<run_id>.json`。

> **已落地**：写入面在 `factory/orchestration/lineage.mjs`，由 `spec/check-lineage.mjs`（`LN-1`..`LN-5`）守护。
> 血缘目录**入库**（不 gitignore）——`§50.2` 要求与代码同版本可追溯，且 append-only 由版本控制的
> 差异过滤器强制。`IN-13`：它与工作流事件（`.workflow-runs/`，已 gitignore）**分开存储**。

### 4.1 必填字段

| 字段 | 含义 | 合规属性 |
|---|---|---|
| `run_id` | 全局唯一标识；贯穿运行时/沙箱/工具面/存储四层，**不得被重新生成**（`CP-79`） | — |
| `started_at` / `ended_at` | UTC 时间戳 | *同步*——禁事后补录；补录须显式标记并说明原因 |
| `agent` | 角色 id（须在 §1 在册）+ 模型标识 | *可归属* |
| `goal` | 一句话目标陈述 | — |
| `action_level` | 本次触及的最高等级 | — |
| `files` | 触及文件路径数组 | *可追溯* |
| `declares` | 声明触及的约束 ID（须在册） | *准确*——与实际 diff 不符即"幻觉" |
| `gates` | 各闸门结果 | — |
| `human_release` | `L2` 必填：放行人 + 方式 + 被放行的改动集标识 | *可归属* |
| `iterations` | 迭代轮数 | 护栏判据 |
| `cost` | token / 计算分钟；**未知填 null 而非 0** | *准确* |

### 4.2 硬要求

- **append-only**：只许新增。写错了 → 新增一条更正记录指向原记录，**不改原件**（`AG-7`）。
- **失败与中止的会话同样落血缘**（`AG-2`）——只记成功会话是有偏采样。
- `files` 含 `L2` 路径面 → `human_release` 不得为空（`AG-9`）。
- `cost` 的空值语义：**0 是一个断言（"没有花费"），null 是一个事实（"未测量"）**（`AG-8`）。
  运行时侧已经这么做了，见 `factory/runtime/interface.mjs` 的 `EMPTY_METERING`。

---

## 5. 护栏

| 护栏 | 取值 | 超限行为 |
|---|---|---|
| 单任务迭代上限 | `25` | **升级给人，不静默重试**（`AG-10` / `RT-8`） |
| 单任务 token 预算 | `200000` | 中止 + 落血缘 |
| 单任务计算分钟预算 | `30` | 中止 + 落血缘 |

**取值理由**（阈值不写理由就等于没定，下一个人会随手改）：

- **25 轮**——实测单轮探针活动约 9.4k input tokens，25 轮落在 token 预算内仍有余量。
  官方 Hosting 文档指出内存随会话长度增长且**没有顶层超时**，25 轮是能在 1 GiB/活动内跑完的量级。
- **200k tokens**——单活动上下文窗口的约 20%，给压缩留足空间。超过说明任务该拆（`AG-18`）。
- **30 分钟**——人能容忍的等待上限。超了通常不是执行能力问题，是任务定义或知识缺口的信号（`AG-20`）。

**当前强制点**：只有迭代上限有运行时强制点（经 `maxTurns` 落到 SDK）。
token 与计算分钟**无强制点**，见 §7 `X-5`——这正是 `§52.2` 说的 C4 缺口。

---

## 6. 硬规矩（违反 = bug）

| id | 规矩 | 理由 |
|---|---|---|
| `AO-1` | **能用确定性规则表达的，禁止交给模型。** | 做成 agent = 把可验证约束换成不可验证约束，还丢掉可复现性 |
| `AO-2` | 元语说的和代码做的不一致时，**停下说明冲突**，不要单方面改任一侧 | 擅自"修正"会破坏已定决策 |
| `AO-3` | 治理只诊断、不自动改。校验器无 `--fix` | 自动修会让执行者跳过"为什么违规" |
| `AO-4` | 看到测试红，**先在干净基线复跑做归属判断**，再动手 | 智能体默认假设自己是唯一变量，会去修一个它没造成的问题 |
| `AO-5` | 新增校验维度必须**同一提交**配负向用例 | 未经负向验证的校验器不计入一致性证据（`FR-3`） |
| `AO-6` | 负向验证报的缺陷**优先于**正向全绿 | 正向绿只说明"当前没违规"，负向红说明"这个维度根本没在守"。本仓的 `RB-2` 漏洞就是这么抓到的 |
| `AO-7` | 改 `providers.mjs` 或 `adapter.mjs` 后**必须跑一次 smoke** | 供应商路由的失效是静默的：闸门全绿、活动照跑，只是跑在了错误的账户上 |

---

## 7. 豁免（**唯一位置**，须给理由与失效条件）

> 无法机器守护的约束**必须**在此在册。
> **登记一条挡不住的禁令，比假装它被挡住要好**——读者由此知道该靠人盯，而不是误以为有闸门。

| 豁免项 | 内容 | 理由（为何无法机器化） | 失效条件 |
|---|---|---|---|
| `X-1` | `L2` 人工放行无平台时间戳与独立放行记录 | 当前放行载体是交互式会话的权限提示，不产生可归属的持久记录（`IN-1` 要求带时间戳与身份的确认点） | 接入 GitHub PR review 或等价放行闸后失效 |
| `X-2` | `F-1`（密钥入库）无机器守护 | 仓内无 secret scanner；`.gitignore` 只挡已知路径，挡不住写进新文件的密钥 | 接入 secret 扫描（pre-commit 或闸门维度）后失效 |
| `X-6` | 血缘 append-only 只对**已提交**记录生效；未提交记录的改动版本库看不见 | 版本控制语义决定的，不是漏洞——记录一旦入库即锁死。`§50.2` 明确要求登记此限制 | 接入具备写时不可篡改语义的存储层后失效 |
| `X-4` | 校验维度的负向覆盖率无机器守护 | 需要从校验器源码提取维度码再与负向用例比对，跨文件静态分析，当前靠 `AO-5` 人工纪律 | 实现维度覆盖率校验后失效 |
| `X-5` | token / 计算分钟预算无运行时强制点 | SDK 只提供 `maxTurns`，无 token 或时长的中止钩子（`§52.2` 的 C4 缺口） | 运行时提供预算强制点后失效 |

---

## 附录：格式契约（校验器解析规则）

1. §1 能力注册表：首列为反引号包裹的角色 id；**至少一行**。
2. §2 动作分级：首列为反引号包裹的四个等级，**四行齐全**；
   `L1`/`L2` 行的路径面 glob **必须能匹配到仓库中真实存在的路径**（防表格腐烂）。
3. §3 禁令：首列为反引号包裹的 `F-n`；每行须有「机器守护」列；
   **无机器守护的禁令必须在 §7 豁免在册**。
4. §4.1 必填字段表：须含 `§27.4` 规定的全部必填字段。
5. §5 护栏表首列非空且三行齐全；§6 硬规矩首列为反引号包裹的 `AO-n`。
6. §7 豁免表首列为反引号包裹的 `X-n`，且须有「失效条件」列。
7. **任一契约表解析为 0 行 = 格式契约破坏 = 红。**
