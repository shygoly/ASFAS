# DECISIONS.md — 决策与不变量登记册（Source of Truth）

> **登记索引 + 对拍点，不是决策过程档案。** 架构级决策（`FADR-n`）的全文在 `spec/ASFAS.html`，
> 本册只登记**实现级**决策与不变量，并作为 [`FLOWS.md`](FLOWS.md) 的 `inv:` 引用面。
>
> **机器校验**：`npm run verify:flows`（已挂进 `npm test`）——
> `FLOWS.md` 里引用的每个 `INV-n` 都须在 §1 在册。
>
> **命名空间**：`INV-n` = 不变量（本册）· `FADR-n` = 工厂架构决策（`spec/ASFAS.html`）·
> `X-n` = 豁免（[`AGENT-OPS.md`](AGENT-OPS.md) §7）。三者正交，禁止混用前缀。

---

## 1. 不变量登记

| id | 不变量 | 出处 | 守护 |
|---|---|---|---|
| `INV-1` | 工作流状态**只能**由事件序列推导，不得另存一份可被直接改写的"当前状态"作为权威 | ASFAS `WF-6` | `deriveState()` 是唯一推导点；`FL-4` 双向对拍 |
| `INV-2` | 决策权不下放：研究工作流的终止 MUST 由自然人触发，智能体只产出候选与依据 | ASFAS `WF-24` / §47.2 | `release` 闸门 + `grantRelease()` 要求 `--by`；`FL-3` |
| `INV-3` | 事件 append-only：已写入的事件不得修改或删除 | ASFAS `WF-6` / `FINV-7` | `EventLog` 只提供 `append()`，无改写方法；进程外限制见 `X-6` |

---

## 2. 已决实现级决策

| id | 决策 | 结论 | 被否决的方案与理由 |
|---|---|---|---|
| `D-EVT-1` | 编排面事件存储载体 | **JSONL 分片文件**（`.workflow-runs/<workflow_id>.jsonl`，追加写） | **SQLite**：在「append-only 强制」判据上不成立——存储引擎原生暴露随机读写，只能靠触发器模拟，而任何写连接都可 DROP TRIGGER 后改写历史。**JSONL 单文件**：在「按 workflow_id 重放」判据上不成立——须顺序扫全量并过滤，成本随总事件量线性增长 |

| `D-LIN-1` | 血缘存储载体 | **版本库内的 append-only 目录** `.agent-runs/<UTC 日期>/<run_id>.json`，强制手段是 `git diff --diff-filter=MD` | **独立数据库**：需另建存储与权限体系，且与代码不同版本，`§50.2` 要求"与代码同版本可追溯"不成立。**接入业务侧不可篡改设施**：会让开发期工具依赖业务表，违反 `FG-4`（项目独立） |
| `D-DISP-1` | Supervisor 派工指令的 typed schema | **分派信封 + 无损投影**（`DispatchInstruction` → `Activity`），形状见下方 | 三条见下方否决表 |
| `D-GRADE-1` | 项目侧分级表的解析契约（工厂怎么读项目的 `AGENT-OPS.md` §2 做归级） | **严格 Schema：精确列头 + 强类型校验**，六条见 §2.3 | 三条见 §2.3 |
| `D-PROJ-1` | 编排面的项目维度与跨项目状态落点 | **ProjectSideStateRegistryGate** + 一条落地限定，见 §2.2 | **FactoryNamespacedState**（四类状态收归工厂仓 `projects/<id>/`）：违反 `FINV-4`——要么无视 `lineage_dir`（项目差异被编排面硬编码位置覆盖），要么为 C2/非 C2 维护两套血缘写路径；且可变项目状态留在工厂侧，隔离只靠路径命名纪律，路径推导 bug 即形成跨项目写路径。**ProjectHookStateDelegation**（落盘路径委托项目侧 hook）：违反 `IN-14`——含 bug 或不受信的项目可把状态写到自身子树之外；C0/C1 未必提供 hook，缺省 **fail-open**，隔离不闭合；且触碰"不得新增对项目侧强制要求" |
| `D-RELEASE-1` | `L2` 活动的路径级人工放行载体（原 `Q-6`） | **先干后放（改动集对拍）**：`L2` 活动在隔离副本执行 → 产出实际改动集 → 归级判 `L2` 面 → 对该**具体改动集**人工放行 → 放行后合并生效 + 血缘回填 `human_release`，见 §2.4 | **阶段级 release 闸复用**：现行 `grantRelease` 是终态（放行即 `workflow.terminated`），没有"放行后继续推进"路径，构成不了 L2 放行载体。**开放路径白名单**（预授权"已放行的路径集"）：违反 `IN-8` ③——放行 MUST 绑定到具体改动集标识，开放白名单无法满足。**先放后干**（派发前按声明的路径放行）：声明可能撒谎，且归级门在派发前无法拿到实际改动集 |
| `D-RELEASE-2` | Test 阶段动作等级（§42 契约 Test=L1 的偏离） | **Test 提为 L2**——MedTrust 分级表 L1 面仅 `apps/api/src/**/*.spec.ts`，connector 测试文件在 `apps/connector/src/domain/**`（L2 面），8 个 RWS 任务目标代码全在 L2 面；Test 阶段写测试文件同样走路径级放行（`D-RELEASE-1` 机制）。偏离 §42 Test=L1 契约，登记为**项目粒度驱动的已知偏离** | **维持 Test=L1**：MedTrust 无任何 L1 面测试目标，Test 阶段将无法产出红测试（写不进 L1 面文件）。**扩 L1 面含测试文件**：改 MedTrust 项目侧分级表，属业务决策且 8 任务全在 connector 域，扩面收益有限 |

> **`D-EVT-1` 的已知代价**（`WF-26` ③）：跨 workflow 的全局查询需扫描所有分片，无内置索引；
> workflow 数量大时产生大量小文件，需管理目录布局与文件句柄。
> 由 `research` 实例 `research-ms8nu3d1` 产出，人（goly）拍板入册——本仓第一条走完完整研究工作流的决策。
>
> **`D-LIN-1` 的已知代价**：append-only 只对**已提交**记录生效，未提交记录的改动版本库看不见。
> 此限制已按 `§50.2` 的明文要求登记为 `AGENT-OPS.md` §7 `X-6`。
>
> **`D-RELEASE-1` 的已知代价**（`WF-26` ③）：① 先干后放要求 `L2` 活动有**隔离工作副本**（`RT-10` 已具备），
> 放行前改动不落在项目工作区——"先干"的成本由隔离副本承担，合并动作须原子；② 改动集收集依赖运行时
> 报告 `changed_paths`（diff 来源），运行时未实现时该面退化（按 `VC-12` 可见跳过，不假阳性）；
> ③ 放行记录与血缘同存项目侧 `.agent-runs/`（`§50.2` 同版本可追溯），放行是**追加事件**非覆盖。

### 2.1 `D-DISP-1` 展开

```
DispatchInstruction {
  run_id?           : string
  target_role       : SemanticRef{kind:'role'}
  task_card         : SemanticRef{kind:'task_card'}
  knowledge_slices  : SemanticRef[]{kind:'knowledge_slice'}
  level_ceiling     : Level            // 标量单值，非区间/数组/联合
  tool_grants       : ToolGrant[]      // 闭集（RT-6）
  input             : JsonValue        // 只承载本次运行的实例化数据
  budget            : { iterations: uint }
}
```

**无损投影到 `Activity`**（判据③）：

```
Activity {
  run_id, role: resolve(target_role), action_level_ceiling: level_ceiling,
  semantic_refs: [task_card, ...knowledge_slices],   // 引用均带 kind 标签，故可区分
  tool_grants, input, budget
}
```

**被否决的三个方案**：

| 方案 | 在哪条判据上不成立 |
|---|---|
| Activity 直扩（`Activity` + `task_card_ref`） | **判据①**：`Activity` 无任务卡一等字段，`role`/`action_level_ceiling` 是运行时已解析值而非派工期的语义引用，§28.3 的四项在类型层面无法区分。且强行追加 `task_card_ref` 并加 `kind` 判别后，schema 已不再是 `Activity` 而是「信封+投影」——**等于承认 `Activity` 不适合充当派工指令形状** |
| 扁平内联（内联任务卡与知识内容） | **判据②**：违反 `FR-17`，副本随源变更分叉（摘要会分叉、引用不会），并失去可解析、可去重、可追溯 |
| 任务卡类型判别联合（tagged union） | **判据③**：各任务卡专有字段被提升到顶层，引擎须为每种类型写专属投影器，schema 无法单方面保证无损映射；任务卡种类增长会改变判别空间，破坏契约稳定性 |

**`AG-18` 三条原子性判据的可机检划分**（判据④）：

| 判据 | 确定性代码可查 | 只能人或模型判 |
|---|---|---|
| ① 能独立过全部闸门 | 任务卡声明的闸门 ∈ 闸门注册表且覆盖该任务类型必选闸门集 · 知识切片引用覆盖任务卡必读输入 · 引用无悬空 | 该 agent 在给定 budget/工具授权下**能否实际通过**各闸门（能力与内容判断） |
| ② 触及路径面归级后是单一等级 | `level_ceiling` 是合法枚举中的标量单值，schema 层强制归级输出基数=1 | 触及路径面的**真实语义归级**是否确实落在该单一等级 |
| ③ 声明的不变量集合自洽 | 语法自洽：重复 · 字面互斥（A 与 ¬A）· 引用未声明符号 · 签名不匹配 | 不变量之间及与知识切片/任务卡上下文的**语义自洽**（隐含时序/状态冲突） |

> **本 schema 只保证确定性子集可自动校验，其余判据 MUST 显式留作评审闸门，MUST NOT 宣称全自动原子性验证。**
> 这句是研究结论自己划的边界，符合 `FP-7`（诚实缺口）——一个声称能全自动验证任务原子性的方案反而该被怀疑。
>
> **`D-DISP-1` 的已知代价**（`WF-26` ③）：① 投影层与两端 schema 须同步维护，`Activity` 每演进投影必须跟随，
> 否则引擎接不上；② 引用解析有间接层，须共享可解析的语义引用命名空间，悬空引用须在派工时快速失败；
> ③ 比 `Activity` 直扩多一层概念，Supervisor 与编排引擎须共同持有该契约。
>
> 落地时代价①可用本仓已有的双向对拍手法守护（参照 `AOP-7` 守 `AGENT-OPS.md` §5 ↔ `guardrails.mjs`）：
> 配一条维度断言投影无损，代价就从"靠纪律"变成"靠闸门"。
>
> 由 `research` 实例 `research-ms8ypc1f` 产出，人（goly）拍板入册。

### 2.2 `D-PROJ-1` 展开

**两道 fail-closed 门**（这是方案的核心，安全由机制而非纪律保证）：

| 门 | 判据 | 缺省行为 |
|---|---|---|
| 等级门（`FINV-3`） | 派发**写类活动**（`action_level` ≥ `L1`）要求 `conformance` ≥ `C2` | `conformance` 缺失视为 `C0` → 拒绝 |
| 归级门（`AG-14`） | 路径归级用**项目自己的**分级表（registry `action_levels` 指向的项目侧文件） | `action_levels` 缺失 → 全路径归 `L2` |

**四类状态的落点**：

| 状态 | 落点 | 依据 |
|---|---|---|
| 血缘 | C2 走 registry 的 `lineage_dir`；非 C2 缺省 `<local_path>/.agent-runs/` | `§50.2`：MUST 与代码**同版本可追溯**，故非在项目仓不可 |
| 工作流事件 | **工厂侧** `.workflow-runs/<project_id>/`（落地限定，见下） | `§50.2` 对事件只要求"有序·持久·可按序重放"，无同版本要求 |
| Checkpoint · Agent Memory | 未落地（`§38` 存储面未定） | — |

> **落地限定（人拍板时追加，非研究结论原文）**：研究推荐的是四类状态**全落项目侧**。
> 但 `§50.1` 对四类的硬要求并不相同——只有血缘要求"与代码同版本可追溯"。
> 事件流放工厂侧按 `project_id` 分目录**不构成 `IN-14` 禁止的跨项目写路径**（每项目一目录，互不可见），
> 且规避了研究自己列出的代价③：往项目树里写运行态可能与项目的 `meta_languages` 扫描、`verify_cmd` 相互干扰。
>
> 这条限定与候选 2 的否决理由**不冲突**：候选 2 被否是因为把**四类全部**收归工厂侧从而无视了 `lineage_dir`；
> 本限定只挪事件流，血缘仍严格走 `lineage_dir`。

> **`D-PROJ-1` 的已知代价**（`WF-26` ③，研究列了 8 条，此处录其要）：
> ① 每次派发须解析 registry 并走两道门，解析失败路径与复杂度增加；
> ② 血缘写路径要求 `local_path` 是可写的项目自有目录，只读或多项目共享时须 fail-closed 拒派；
> ③ `tool_grants` 由分级表按路径推导，映射/解析错误面较大；
> ④ 工厂侧聚合改为跨项目**只读**扫描，引入延迟与一致性成本；
> ⑤ `--project` 成为写类派发的前置，既有不带该参数的调用在写类活动上直接拒绝；
> ⑥ 非 C2 项目血缘缺省落 `<local_path>/.agent-runs/`，与 C2 的 `lineage_dir` 语义可能不一致，聚合侧需归一化层。
>
> 由 `research` 实例 `research-ms90yegl` 产出，人（goly）拍板入册并追加落地限定。

### 2.3 `D-GRADE-1` 展开

**解析契约六条**（落在 `factory/orchestration/grading.mjs`，由 `check-projects.mjs` `PJ-6` 守）：

| # | 规则 |
|---|---|
| ① | **定位**：registry `action_levels` 的前导路径 token = 项目侧文件（相对 `local_path`）。取不出 token → 归级表缺失 |
| ② | **小节**：该文件中首个匹配 `^##+\s*2\.` 的标题，正文到下一个 `##` 为止；小节内**第一张** markdown 表格参与解析 |
| ③ | **精确列头**：表头逐格全等于 `等级` / `范围（路径面）` / `策略`。**不做别名归一化** |
| ④ | **等级闭集**：`L0-readonly` / `L1-low-write` / `L2-high-write` / `L3-forbidden` 四行齐全、无重复、无表外等级 |
| ⑤ | **路径面**：L1/L2 行第二列中反引号包裹且形似路径的 token 构成该等级路径面；**为空即整表不可用** |
| ⑥ | 任一条不成立 → 整表判不可用，门 fail-closed 拒派；**MUST NOT 兜底解析出"能解析的部分"** |

`isPathLike` 与 `spec/check-agent-ops.mjs` 同一形态——工厂读别人的表和查自己的表，判据不一致就是两套标准（`FP-1`）。

**被否决的三个方案**：

| 方案 | 在哪条判据上不成立 |
|---|---|
| **别名映射归一化**（列头归一 + 同义词词典 + 兜底）——研究的原推荐 | 它自己列出的代价⑤：**同一张表在不同词典版本下归级可能漂移**。下游是"agent 拿不拿得到写权限"，启发式消歧（「级别」指等级还是优先级）在权限门上是静默错判 |
| **LLM 语义抽取** | K1 确定性 / K4 运行代价：同一 §2 在不同温度或模型版本下可能产出不同归级，且无法为失败给出可复现的字段级归因（`AO-1`：能用确定性规则表达的禁止交给模型） |
| **逐项目声明式选择器**（解析规则由项目自声明） | K3 失败可诊断：工厂无法独立校验"选择器配置"与 §2 实际内容一致；且回避了"配置自身如何解析"，未收敛变体反而扩散 |

> **`D-GRADE-1` 的已知代价**（`WF-26` ③）：① 既有项目的 §2 表格须与契约完全一致，格式漂移即整表不可用（**代价可测**：落地时实测本仓与两个在册项目的 `AGENT-OPS.md` 全部一次通过，迁移成本为 0，但这只说明当前，不保证将来）；
> ② 契约由工厂单方面规定，项目侧改表需与工厂契约同步，跨仓协调成本落在人身上；
> ③ 严格解析对**合法的表达变体**（换列名、加一列说明）零容忍，误拒的代价是多一次人工放行而非误放行——方向是对的，但会产生摩擦。
>
> **推荐项被人推翻**：研究推荐的是别名归一化，人（goly）改选严格 Schema。理由是研究把 K2（兼容既有非规范表格）权重定得过高——
> 在册项目只有 3 个且元语格式本就由工厂规定，"改写表格"是一次性可见代价，而漂移是永久且静默的。
> 由 `research` 实例 `research-ms9ou3a7` 产出候选，人拍板选定**非推荐项**。

### 2.4 `D-RELEASE-1` 展开（原 `Q-6`）

**先干后放（改动集对拍）的要点**：

1. `L2` 活动在**隔离工作副本**执行（`RT-10`：`action_level_ceiling ≥ L1` 时按 `run_id` 隔离 `cwd`/`CLAUDE_CONFIG_DIR`，已实现）。放行前改动不落在项目工作区。
2. 活动完成后运行时报告**实际改动集** `changed_paths`（diff 来源）。归级门用它对拍项目分级表——改动集含 `L2` 面即触发路径级放行请求。
3. 请求挂在**具体改动集**上（`IN-8` ③：放行绑定到改动集标识，不是分支/任务/开放路径）。人放行后合并生效 + 血缘回填 `human_release`（`AG-9`）。
4. 放行是**追加事件**（`path-release.granted`），与血缘同存项目侧 `.agent-runs/`（`§50.2`）。

**落地限定**（人拍板追加）：`tool_grants` 由分级表按路径面推导（`D-PROJ-1` 已知代价③ 既定方向），不硬编码空集。

**已知代价**（`WF-26` ③）：隔离副本成本、改动集收集依赖运行时实现、合并须原子。

由 `orchestrate` 实例 `orchestrate-ms9xolqd`（8 项 RWS 缺口任务）暴露的派发能力缺口触发，人（goly）拍板。

---

## 3. 未决段

| id | 未决项 | 决出前禁止什么 |
|---|---|---|
| `Q-2` | 并行拆分时的隔离要求（依赖 ASFAS `QA-4`） | 禁止在一个工作流实例内并行派发多个活动 |

## 4. 已销项索引

| id | 原未决项 | 结论 | 产出实例 |
|---|---|---|---|
| `Q-1` | 派工指令的完整 typed schema | `D-DISP-1` | `research-ms8ypc1f` |
| `Q-3` | 血缘写入面的存储载体 | `D-LIN-1` | `research-ms8nu3d1` |
| `Q-4` | 编排面的项目维度与跨项目状态落点 | `D-PROJ-1` | `research-ms90yegl` |
| `Q-5` | 项目侧分级表的解析契约 | `D-GRADE-1` | `research-ms9ou3a7` |
| `Q-6` | 路径级人工放行闸：`L2` 活动的放行载体 | `D-RELEASE-1` | `orchestrate-ms9xolqd`（8 项 RWS 缺口任务的缺口分析） |
